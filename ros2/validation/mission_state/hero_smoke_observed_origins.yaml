/**:
  ros__parameters:
    agent_ids: [Drone1, Husky1]
    vehicle_types: [drone, ugv]
    odom_topics:
      - /hercules_drone/Drone1/ground_truth/odom_local
      - /hercules_ugv/Husky1/ground_truth/odom_local

    # World-NED locations at which the wrapper's local odometry was zero in
    # the 2026-09-08 live run. These differ from the requested settings spawn
    # positions after physics settling/local-estimator initialization. They are
    # validation evidence, not portable RuralAustralia spawn constants.
    vehicle_origins_ned:
      - 0.0
      - -3.0
      - 0.8238999247550964
      - 0.004974978044629097
      - 3.001892566680908
      - 0.7162424921989441

    state_topic_prefix: /hercules_mission/ground_truth
    freshness_timeout_sec: 0.5
