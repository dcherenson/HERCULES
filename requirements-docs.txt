# Build the HERCULES documentation site (MkDocs + Material):
#   pip install -r requirements-docs.txt
#   mkdocs serve     # preview at http://localhost:8000
#   mkdocs build     # render static site to ./site
mkdocs>=1.6
mkdocs-material>=9.5
pymdown-extensions>=10
